<Project Sdk="Microsoft.NET.Sdk">

	<!-- MSBuild project settings -->
	<PropertyGroup>
		<TargetFramework>net7.0</TargetFramework>
		<RootNamespace />
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
		<Nullable>enable</Nullable>
		<GenerateDocumentationFile>flase</GenerateDocumentationFile>
	</PropertyGroup>

	<ItemGroup>
		<EmbeddedResource Include="$(MSBuildThisFileDirectory)/**/*.cs" />
	</ItemGroup>

</Project>
